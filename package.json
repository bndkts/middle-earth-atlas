{
  "name": "middle-earth-atlas",
  "version": "1.0.0",
  "private": true,
  "description": "An interactive map, timeline, and journey explorer for Middle-earth.",
  "license": "MIT",
  "scripts": {
    "test": "node --check src/data.js && node --check src/images.js && node --check src/app.js && node scripts/validate-data.mjs && node --test scripts/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
